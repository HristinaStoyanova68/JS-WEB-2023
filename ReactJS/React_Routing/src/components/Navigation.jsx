const Navigation = () => {
    return (
        <nav>
            <a href="/">Home</a>
            <a href="/about">About</a>
            <a href="/contacts">Contacts</a>
        </nav>
    );
}

export default Navigation;